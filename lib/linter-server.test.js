//write test that confirms post endpoint accepts the post body and returns statement that linter passes or not.

describe('Tests will verify the post endpoint of the linter-server is working correctly', () => {
  it('test verify linter is working', () => {
    
    expect().toEqual();
  });
});